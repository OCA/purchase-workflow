* Lines are paired by product only. An alternative quoting the same need
  through a different product -- an equivalent article from another vendor --
  is not linked, and has to be connected by hand.
* Links are added, never removed. Detaching an alternative from an RFQ leaves
  the request line pointing at it.
