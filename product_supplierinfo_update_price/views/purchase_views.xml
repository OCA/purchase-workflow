<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_form" model="ir.ui.view">
        <field
            name="name"
        >purchase.order.form (in product_supplierinfo_update_price)</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='price_unit']"
                position="after"
            >
                <field name="can_update_product_supplierinfo" invisible="1" />
                <button
                    name="action_update_product_supplierinfo"
                    type="object"
                    icon="fa-refresh"
                    title="Update Product Supplierinfo Price"
                    attrs="{'invisible': ['|', ('id', '=', False),('can_update_product_supplierinfo', '=', False)]}"
                />
            </xpath>
        </field>
    </record>

</odoo>
