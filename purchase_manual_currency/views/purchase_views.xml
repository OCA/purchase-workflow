<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='currency_id']" position="after">
                <label
                    for="manual_currency"
                    groups="base.group_multi_currency"
                    invisible="currency_id == company_currency_id"
                />
                <div
                    groups="base.group_multi_currency"
                    invisible="currency_id == company_currency_id"
                >
                    <field name="manual_currency" readonly="state != 'draft'" />
                    <label for="currency_rate" invisible='1' />
                    <div class="o_row" invisible="not manual_currency">
                        <field
                            name="currency_rate"
                            readonly="state != 'draft'"
                            digits="[12,12]"
                        />
                        <field
                            name="type_currency"
                            required="manual_currency"
                            readonly="state != 'draft'"
                        />
                        <button
                            aria-label="Opened"
                            type="object"
                            name="action_refresh_currency"
                            class="btn-link"
                            invisible="state != 'draft'"
                        >
                            <i title="Update Currency" class="fa fa-fw fa-refresh" />
                        </button>
                    </div>
                </div>
            </xpath>
            <xpath
                expr="//field[@name='order_line']//list/field[@name='price_subtotal']"
                position="after"
            >
                <field name="company_currency_id" column_invisible="1" />
                <field
                    name="subtotal_company_currency"
                    column_invisible="parent.company_currency_id == parent.currency_id"
                    optional="show"
                    string="Amount (Company Currency)"
                />
            </xpath>
        </field>
    </record>
</odoo>
