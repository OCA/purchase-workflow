<?xml version="1.0"?>
<odoo>

    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase_order_form_inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='action_view_invoice']" position="before">
                <button type="object"
                        name="action_view_all_pickings"
                        class="oe_stat_button"
                        icon="fa-truck"
                        attrs="{'invisible': [('all_picking_count', '=', 0)]}">
                    <field name="all_picking_count" widget="statinfo" string="All Pickings" help="All Pickings, including chained"/>
                </button>
            </xpath>

            <xpath expr="//button[@name='action_view_picking']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

        </field>
    </record>

</odoo>
